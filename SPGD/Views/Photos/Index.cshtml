@model IEnumerable<SPGD.Models.Photo>

@{
    ViewBag.Title = "Index";
}

<h2>Collection des photos (Page Temporaire)</h2>

<p>
    @Html.ActionLink("Téléverser des photos", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Location de l'image
        </th>
        <th>
            Status de la Séance
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PhotoPathName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Seance.StatusSeance)
        </td>
       @* <td>
           @Html.ActionLink("Modification", "Edit", new { id=item.PhotoID }) |
    @Html.ActionLink("Details", "Details", new { id=item.PhotoID }) |
    @Html.ActionLink("Delete", "Delete", new { id=item.PhotoID })
        </td>*@
    </tr>
}

</table>
